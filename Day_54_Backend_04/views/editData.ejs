<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>First Backend Program</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <link rel="stylesheet" href="/stylesheets/style.css" />
  <body>
    <div class="main p-10 w-full min-h-screen bg-zinc-900">
      <h1 class="text-5xl text-zinc-400 text-center pt-8">Lazi's Note Store</h1>
      <a class="mb-10 text-blue-500 inline-block" href="/">Go Back to Home</a>

      <div class="form text-white p-10">
        <form action="/editData" method="post">
          <h3 class="mb-2">Editing : <%= taskname %></h3>
          <input type="text" name="taskname" value="<%= taskname %>" hidden />
          <textarea
            name="details"
            class="px-5 py-2 bg-zinc-800 mt-2 w-full outline-none rounded-md"
            style="height: 180px"
            placeholder="Add more notes.."
            value="<%= taskdata %> "
          >
          <%= taskdata %> 
        </textarea
          >

          <input
            type="submit"
            value="Update"
            class="mt-2 block bg-green-600 px-5 py-2 rounded-md"
          />
        </form>
      </div>
    </div>
  </body>
</html>
