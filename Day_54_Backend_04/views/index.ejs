<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>First Backend Program</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <link rel="stylesheet" href="/stylesheets/style.css" />
  <body>
    <div class="main w-full min-h-screen bg-zinc-900">
      <h1 class="text-5xl text-zinc-400 text-center pt-8">Lazi's Note Store</h1>
      <div class="form text-white p-10">
        <form action="/create" method="post">
          <input
            class="block px-5 py-2 bg-zinc-800 w-full rounded-md outline-none"
            type="text"
            name="title"
            placeholder="Title of your note.."
          />
          <textarea
            name="details"
            class="px-5 py-2 bg-zinc-800 mt-2 w-full outline-none rounded-md"
            style="height: 150px"
            placeholder="Your notes here.."
          ></textarea>
          <input
            type="submit"
            value="Add Task"
            class="mt-2 block bg-blue-600 px-5 py-2 rounded-md"
          />
        </form>
      </div>
      <div class="tasks p-10 flex gap-3 flex-wrap">
        <% if(files.length > 0){ %> <% files.forEach((task) => { %>

        <div class="task min-w-80 px-3 py-4 bg-zinc-800 rounded-md">
          <h1 class="text-white tracking-tighter text-3xl"><%= task %></h1>
          <div class="flex justify-between mt-3">
            <a
              class="text-blue-500 inline-block content"
              href="/yourTask/<%= task %>"
              >View Task</a
            >
            <a class="text-zinc-300" href="/edit/<%= task %>">Edit Taskname</a>
          </div>
        </div>

        <% }) %> <% } else { %>

        <h3 class="text-zinc-500 px-10">No any tasks added yet..</h3>

        <% } %>
      </div>
    </div>

    <script src="/javascripts/script.js"></script>
  </body>
</html>
